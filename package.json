{
  "name": "git-ninja",
  "displayName": "Git Ninja",
  "description": "Advanced Git management tools: exclude files, skip-worktree, checkout from branches, cleanup repositories, and clean ignored files with ease.",
  "version": "1.1.0",
  "publisher": "KabirKhyrul",
  "icon": "icon.webp",
  "author": {
    "name": "Git Ninja Team"
  },
  "license": "MIT",
  "homepage": "https://github.com/kabirkhyrul/git-ninja",
  "repository": {
    "type": "git",
    "url": "https://github.com/kabirkhyrul/git-ninja.git"
  },
  "bugs": {
    "url": "https://github.com/kabirkhyrul/git-ninja/issues"
  },
  "keywords": [
    "git",
    "exclude",
    "skip-worktree",
    "checkout",
    "branch",
    "cleanup",
    "clean",
    "prune",
    "version control",
    "file management",
    "ignore files",
    "git tools",
    "repository management",
    "gitignore",
    "orphaned branches"
  ],
  "engines": {
    "vscode": "^1.102.0"
  },
  "categories": [
    "SCM Providers",
    "Other"
  ],
  "activationEvents": [
    "onCommand:git-exclude.toggle-exclude",
    "onCommand:git-exclude.toggle-skip-worktree",
    "onCommand:git-exclude.checkout-from-branch",
    "onCommand:git-exclude.cleanup",
    "onCommand:git-exclude.clean-ignored",
    "onCommand:git-exclude.prune-remotes",
    "onCommand:git-exclude.clean-untracked-ignored"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "git-exclude.toggle-exclude",
        "title": "Git: Toggle .git/info/exclude"
      },
      {
        "command": "git-exclude.toggle-skip-worktree",
        "title": "Git: Toggle Skip-Worktree (Ignore Local Changes)"
      },
      {
        "command": "git-exclude.checkout-from-branch",
        "title": "Git: Checkout File from Branch"
      },
      {
        "command": "git-exclude.cleanup",
        "title": "Git: Cleanup (Prune Remotes & Delete Orphaned Branches)"
      },
      {
        "command": "git-exclude.clean-ignored",
        "title": "Git: Clean Ignored Files (.gitignore)"
      },
      {
        "command": "git-exclude.prune-remotes",
        "title": "Git: Prune Remotes & Delete Safe Local Branches"
      },
      {
        "command": "git-exclude.clean-untracked-ignored",
        "title": "Git: Clean Untracked & Ignored Files"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "git-exclude.toggle-exclude",
          "when": "resourceScheme == file",
          "group": "1_git"
        },
        {
          "command": "git-exclude.toggle-skip-worktree",
          "when": "resourceScheme == file",
          "group": "1_git"
        },
        {
          "command": "git-exclude.checkout-from-branch",
          "when": "resourceScheme == file",
          "group": "1_git"
        }
      ],
      "scm/resourceState/context": [
        {
          "command": "git-exclude.toggle-exclude",
          "group": "1_git"
        },
        {
          "command": "git-exclude.toggle-skip-worktree",
          "group": "1_git"
        },
        {
          "command": "git-exclude.checkout-from-branch",
          "group": "1_git"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node",
    "watch": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --watch",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/vscode": "^1.102.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "eslint": "^9.25.1",
    "esbuild": "^0.25.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.8.3",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2"
  }
}